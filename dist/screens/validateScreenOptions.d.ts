import { NavigationScreenOptions } from '../screens';
import { NavigationRoute } from '../types';
/**
 * Make sure screen options returned by the `getScreenOption`
 * are valid
 */
export default function validateScreenOptions(screenOptions: NavigationScreenOptions, route: NavigationRoute): void;
